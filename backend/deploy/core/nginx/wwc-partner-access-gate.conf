# Include only in the wildcard personal-partner server block.
# Never include in wwc.best, www, staging, admin or other technical vhosts.
if ($wwc_partner_access_allowed = 0) {
    return 302 https://wwc.best$uri;
}
