# Состояние: подключение партнёра (PRO-статус, инструкция, клуб, оплата)

План: `PLAN.md` в этой папке. Решения владельца 13.09: PRO (сайт) / CLUB / настройка сайта (разово); валюта WWC$, 1 W$ = 1 WWC$ = 100 ₽, без «баллов»; клуб 40 WWC$/мес (120 за 3 мес), настройка сайта 20 WWC$; бонус рефереру только с PRO (20 %/10 %); клуб = учёт + напоминания; ручная работа агента — правило B (команды бота, API, синхронизация; SQL только чтение/миграции).

## Фаза A — на production 13.09 14:40 МСК (Core `9d42097`, сайт `6c51134`)

| Что | Где | Ревизия |
|---|---|---|
| Core: `/me.account` (баланс, PRO, CLUB), `/start pro` | staging-api (`staging.wwc.best/api`) | `e36d15f` (ветка `feat/partner-connection`) |
| Сайт: карточка статуса, замки PRO, CLUB на «Академии» | `staging.wwc.best` | `ddeead3` (ветка `feat/pro-status-card`) |

Проверено: гость — карточки нет, две плашки PRO с замком, клик → объяснение с кнопками; вошедший с PRO — карточка `12,5 WWC$ · PRO 84 дн. · CLUB 5 дн.`, плашек нет (проверено с подменённым ответом `/me` в браузере и на живом staging по разметке). `staging.wwc.best/api/v1/content-access/me` идёт в staging-api.

Что показать владельцу на staging: (1) открыть меню гостем; (2) нажать плашку PRO; (3) войти через Telegram — карточка со своими данными.

Правки владельца по ходу: замки настоящие (калькулятор, оба прайса, дизайн); вместо плиток PRO/CLUB — одна капсула баланса в шапке (серый 0 / золотой `2,5k WWC$`); плашки одной ширины; шапка меню 58 px, плитки в прокрутке, нижняя полоса откреплена после входа; анкета — одна строка «Отвечу в течение дня.» и все шаги без прокрутки на 360×640. Staging nginx теперь отдаёт HTML с `no-cache` (как прод). Ветки задачи удалены, `master` = `origin/master` в обоих репо.

## Фаза B — на production (сайт `9e1ace4`, Core `ef7aad8`, 15.09)

Реферальная привязка на всех внутренних ссылках (`?ref=`), футер: реквизиты текстом, мельче/серее; стандарт `docs/PARTNER_ONBOARDING_STANDARD_V1.md` сайта дополнен (таблица: агент пишет сам, только значения из списков; `Referral_Bonuses` — зеркало бонусов).

## Фаза C — на production 15.09 (Core `ef7aad8`)

Продукты CLUB / настройка сайта / пакет PRO+клуб, персональные цены, многострочная `оплата`, напоминания клуба. Оплатившие партнёры: zinaida, kira, olga-samtsova, sofiya — PRO и CLUB (у Киры — настройка сайта) до 21.12.2026 включительно по условию акции. Долги — `DEBTS.md`.

## Этап 4 (ТЗ §19) — тоннель поддержки: staging `dc931c7`, 13.09 20:20 МСК

Решения владельца: сначала тоннель, кнопки сервисов потом; кнопки «Спросить» нет, ссылок нет; карточка Gemini Pro с двумя предложениями (18 мес — 4 490 ₽, гарантия 1 мес; 1 год — 6 900 ₽, гарантия на весь срок) и кнопкой «Поддержка»; перед заказом подтверждение «Заказать / Отменить». Администратор сервиса: на staging — Виктор (688931415), на бою — Карина @karina_bogatnko (2101187096). Id задаётся `PLATFORM_SUPPORT_ADMIN_TELEGRAM_ID` в `.env` окружения (одна БД на staging и prod — в таблицу класть нельзя). Миграция `platform_support_tickets_v8.sql` применена к общей БД.

Механика ответа администратора: Reply на сообщение с шапкой `#S-NNNN` → бот по `reply_to_message.message_id` находит обращение; без Reply — в единственное открытое, при нескольких бот просит Reply. Кнопка «Закрыть #S-NNNN» у каждого сообщения администратору.

Как проверить на staging (`@wwc_admin_staging_bot`): со второго аккаунта (`@khrypko_pro`) написать боту «сервисы» → выбрать вариант → «Заказать»; с основного аккаунта (688931415) ответить через Reply; закрыть кнопкой. Перед переносом на бой Карина должна нажать `/start` у `@WHIEDA_Advisor_bot`.

## Этап 4 на production — 15.09 12:12 МСК (Core `3652b0b`, staging тот же SHA)

Решение владельца 15.09: администратор видит только «Клиент WWC · Заявка #S-N» — без имени, фамилии, username и ссылки (настоящее имя остаётся в `support_tickets.user_display` для владельца). Команда «сервисы» работает на бою (профиль `minimal`), AGENTS.md дополнен. Карина (2101187096) нажала `/start` у `@WHIEDA_Advisor_bot` — привязка есть; `PLATFORM_SUPPORT_ADMIN_TELEGRAM_ID` на бою = 2101187096. Тестовая заявка #S-1 на staging (Виктор ↔ Виктор) осталась открытой — закрыть кнопкой.

Форум-группа (решение владельца 15.09, группа `https://t.me/+Dip8sVVP9eJjMGY6`, бот — админ): миграция `platform_support_forum_v9.sql` применена к общей БД; на каждую новую заявку бот создаёт тему «#S-N · Gemini …», клиентские сообщения падают в тему, Карина пишет в теме без Reply (👍 от бота = доставлено), «Закрыть #S-N» кнопкой или словом «закрыть» — тема переименовывается в «✅ …» и закрывается. Регистрация: владелец или Карина пишет `/forum` в группе (бот должен получать сообщения группы — он админ). Без зарегистрированной группы или при ошибке создания темы заявка идёт по старому пути в личку администратора. При `/forum` открытые заявки этого окружения (по id администратора) переносятся в темы с историей переписки. Первый `/forum` 15.09 14:22 МСК не сработал — вебхук отбрасывал групповые сообщения до процессора (`telegram_group_message_ignored`); исправлено: форумный трафик (`/forum`, сообщения в темах, кнопка «Закрыть») пропускается в Core. Второе предложение Gemini заменено: 6 месяцев — 3 990 ₽ (годовой с гарантией на весь срок сейчас недоступен).

## Аудит советника после миграции на Core — 15.09 (Core `160053b` на staging)

Метод: прогон golden-набора (`qa/telegram_golden`, 274 кейса) через `handle_structured_query` в контейнере staging — `qa/telegram_golden/lab/live_replay.py`. До правок: **123/274 красных**. Найдено и починено:
1. `NameError: _PRODUCT_COLUMNS` — потерян при восстановлении 02.09 (`86ed285`); любой вопрос, совпавший с алиасом «набора» (активатор, пептид, линчжи, сауна, очки…), падал в generic-ошибку, следом терялся контекст для «цена/фото». 35 кейсов. (`4ead2ba`)
2. Потерян guard `8f08a74`: вопрос по конкретному товару («сколько стоит активатор клеток») отвечал набором «поддержка животного». (`160053b`)
После: **70/274**. Остаток — не поломки, а качество: (а) fallback «Я лучше всего помогаю…» вместо уточнения по товару/контексту — работа над `gap.py` (оценка владельца 3–5 дней); (б) «какие есть товары / каталог» — текст вместо списка; (в) уточнение с одним вариантом («pro» → «выберите: Активатор PRO»); (г) «цена сауны» при контексте активатора отвечает активатором; (д) корпус: «Для партнера» без ё, приветствие «Здравствуйте», синтетические «товар без фото». Данные знаний синхронизируются (n8n «WHIEDA Structured Sync Cron», каждые 15 мин, success); gap-событий за 14 дней всего 8 — трафик к советнику маленький.
Красные юнит-тесты (известные, не трогал): `test_advisor_sql_engine::test_engine_price_without_product_returns_clarification`, `::test_explicit_pro_photo_not_overwritten_by_comparison_context` (та же тема fallback); сбор `test_company_knowledge_pack.py`, `test_telegram_golden_{http,master_parity,surface_triage}.py` (потерян `qa/telegram_golden/lab/surface.py`).

## Фаза D — Gemini-продажи: на staging 16.09 (Core `969ca15`), миграция v10 применена к общей БД

Готово: тариф (`тариф gemini_6m 3990 2990 2490 5`), «Оплачено» → тариф → кто продал (реферер клиента подсказывается e-mail-логином) → списание с депозита, партнёру 5 WWC$ + пуш, строка в теме «Бонусы», копия Виктору; «Активировано на N мес» → клиенту дата, заявка закрыта; «перевёл N» (Виктор) → «Получила» (Карина) → депозит; «баланс», «отчёт» (месяц: продажи, депозит, по партнёрам по e-mail); ежедневно 09:00 МСК — напоминание клиенту за 7 дней до конца лицензии и предупреждение о депозите меньше одной лицензии (в тему «Отчёты»). Темы «Бонусы»/«Отчёты» бот создаёт при `/forum`. Не сделано: зеркало начислений Gemini в `Referral_Bonuses` (скриптом агента, по требованию); `lead_actors.email` заполняется из формы/владельцем — пока у партнёров почты нет, в ленте «партнёр без почты · xxxxxx».

Замечание к процессу: 16.09 утром NSP-агент влил в master `7519a63` (393 файла, в т.ч. `routes.py`, `processor.py`, `delivery.py`, `advisor/sql/repository.py`, `.pyc`): гейт покраснел (`test_schema_requirements`), staging — 238/274 golden красных (`UndefinedColumn retail_prices`). Починено: таблицы добавлены в карту, `advisor_gap_review_*` и колонка `retail_prices` применены к общей БД, `.pyc` убраны; golden снова 75/274. Прод остаётся на `99cccaa` до проверки фазы D.

## 19.09 — «WWC Bot» и дотяжка путей (Core `eeb0be8` на staging и бою)

Решение владельца: старые кнопки главной — в одну «🤖 WWC Bot» в кабинете; /start везде открывает кабинет; staging и бой — один код (разница только `PLATFORM_TELEGRAM_UI_PROFILE`: командное меню и ручные операции партнёров). Обход всех путей скриптом (`scratchpad/bot_paths.py`, доставка замокана) на staging и бою — 40 путей, все отвечают. Починено по обходу: (1) на бою команды владельца «оплата/цена/статус» падали в советника (19.09 05:23 «оплата ref:fedorov 3000 rub 3» — unrouted); (2) любой человек, написавший «цена» после карточки, получал billing «forbidden» — теперь советник; (3) «/me» сайта 500 при двух профилях у одного человека; (4) команды депозита в общей теме группы отбрасывались; (5) панель показывала «Отзывы и материалы» без кнопки. Тестовая заявка #S-1 на staging закрыта (system).

Остаток по боту: качество fallback советника (gap.py, 3–5 дней); e-mail партнёров (0 в Core) — «партнёр без почты» в ленте «Бонусы»; командное меню бота на бою — 4 команды (minimal), на staging — 10: если владелец хочет одинаково — одна правка `MINIMAL_TELEGRAM_MENU_COMMANDS`; 13 тестовых PRO истекают 21.09.

## Открытые вопросы владельцу
- Реквизиты в `renewal_requests.py:83-85` (+7 928 237-26-77 Т-Банк / SUNRAYSWORD) — актуальны?
