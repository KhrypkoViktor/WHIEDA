# Состояние: подключение партнёра (PRO-статус, инструкция, клуб, оплата)

План: `PLAN.md` в этой папке. Решения владельца 13.09: PRO (сайт) / CLUB / настройка сайта (разово); валюта WWC$, 1 W$ = 1 WWC$ = 100 ₽, без «баллов»; клуб 40 WWC$/мес (120 за 3 мес), настройка сайта 20 WWC$; бонус рефереру только с PRO (20 %/10 %); клуб = учёт + напоминания; ручная работа агента — правило B (команды бота, API, синхронизация; SQL только чтение/миграции).

## Фаза A — на production 13.09 14:40 МСК (Core `9d42097`, сайт `6c51134`)

| Что | Где | Ревизия |
|---|---|---|
| Core: `/me.account` (баланс, PRO, CLUB), `/start pro` | staging-api (`staging.wwc.best/api`) | `e36d15f` (ветка `feat/partner-connection`) |
| Сайт: карточка статуса, замки PRO, CLUB на «Академии» | `staging.wwc.best` | `ddeead3` (ветка `feat/pro-status-card`) |

Проверено: гость — карточки нет, две плашки PRO с замком, клик → объяснение с кнопками; вошедший с PRO — карточка `12,5 WWC$ · PRO 84 дн. · CLUB 5 дн.`, плашек нет (проверено с подменённым ответом `/me` в браузере и на живом staging по разметке). `staging.wwc.best/api/v1/content-access/me` идёт в staging-api.

Что показать владельцу на staging: (1) открыть меню гостем; (2) нажать плашку PRO; (3) войти через Telegram — карточка со своими данными.

Правки владельца по ходу: замки настоящие (калькулятор, оба прайса, дизайн); вместо плиток PRO/CLUB — одна капсула баланса в шапке (серый 0 / золотой `2,5k WWC$`); плашки одной ширины; шапка меню 58 px, плитки в прокрутке, нижняя полоса откреплена после входа; анкета — одна строка «Отвечу в течение дня.» и все шаги без прокрутки на 360×640. Staging nginx теперь отдаёт HTML с `no-cache` (как прод). Ветки задачи удалены, `master` = `origin/master` в обоих репо.

## Фаза B — в работе
## Фаза C — не начата

## Этап 4 (ТЗ §19) — тоннель поддержки: staging `dc931c7`, 13.09 20:20 МСК

Решения владельца: сначала тоннель, кнопки сервисов потом; кнопки «Спросить» нет, ссылок нет; карточка Gemini Pro с двумя предложениями (18 мес — 4 490 ₽, гарантия 1 мес; 1 год — 6 900 ₽, гарантия на весь срок) и кнопкой «Поддержка»; перед заказом подтверждение «Заказать / Отменить». Администратор сервиса: на staging — Виктор (688931415), на бою — Карина @karina_bogatnko (2101187096). Id задаётся `PLATFORM_SUPPORT_ADMIN_TELEGRAM_ID` в `.env` окружения (одна БД на staging и prod — в таблицу класть нельзя). Миграция `platform_support_tickets_v8.sql` применена к общей БД.

Механика ответа администратора: Reply на сообщение с шапкой `#S-NNNN` → бот по `reply_to_message.message_id` находит обращение; без Reply — в единственное открытое, при нескольких бот просит Reply. Кнопка «Закрыть #S-NNNN» у каждого сообщения администратору.

Как проверить на staging (`@wwc_admin_staging_bot`): со второго аккаунта (`@khrypko_pro`) написать боту «сервисы» → выбрать вариант → «Заказать»; с основного аккаунта (688931415) ответить через Reply; закрыть кнопкой. Перед переносом на бой Карина должна нажать `/start` у `@WHIEDA_Advisor_bot`.

## Открытые вопросы владельцу
- Реквизиты в `renewal_requests.py:83-85` (+7 928 237-26-77 Т-Банк / SUNRAYSWORD) — актуальны?
