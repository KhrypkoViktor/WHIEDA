# STATE

Дата: 2026-09-12

## Готово

- Проверены коммиты `9846e18`, `f7c911b`, `af9fc3d`, `ffdc5b3`, `9504441`, `4666217`.
- Найдена причина полной тишины бота: production webhook был недоступен из-за
  restart loop общего nginx, вызванного конфигом NSP staging.
- Webhook восстановлен; внешний Core health 200; pending updates: 0.
- Подтверждено: текущий кабинет и заявка на сайт существуют, но UX продления,
  список рефералов и поддержка еще не завершены.
- Младшая модель использована только для проверки пользовательских текстов.
- Feature-ветка опубликована в GitHub; текущая ревизия: `7c17c07`.

## Реализовано в ветке

- Блок 1: кабинет, готовое приглашение, Telegram share, поддержка и список
  последних рефералов.
- Команды `/cabinet`, `/invite` и `/support` больше не перехватываются открытой
  заявкой на сайт.
- Блок 2: продление 3/6/12 месяцев, выбор BY/RU, загрузка чека и подтверждение
  владельцем с повторным использованием платежного ledger и бонусов.
- Блок 3: три идемпотентных напоминания, журнал доставок и daily systemd timer.
- Добавлены проверка Telegram webhook, безопасный установщик nginx-конфигов с
  rollback и runbook по инциденту 2026-09-12.
- Незавершенная заявка на сайт или продление больше не перехватывает команды
  `/products`, `/match`, `/calculator` и другие пункты стандартного меню.
- В каждый шаг продления добавлена кнопка `Отменить`.
- Добавлен безопасный owner-canary настоящей команды Telegram без тестовой оплаты.

## Сейчас

Ревизия `7c17c07` развернута только на staging Core. Готовность API: 200, база
доступна, outbox пуст. Staging webhook активен, очередь 0, ошибок Telegram нет.
Меню из 10 команд применено к `@wwc_admin_staging_bot`.

Профильная проверка: 91 тест пройден. Реальные canary `/cabinet` и `/invite`
доставлены аккаунту `@sunraysword`; маршруты `referral` и `referral_invite`.
Production Core и `@WHIEDA_Advisor_bot` не обновлялись.

## Дальше

Виктор проверяет кнопки в `@wwc_admin_staging_bot`. После его подтверждения:

1. отдельный production deploy;
2. повторный owner-canary `/cabinet` и `/invite`;
3. только затем отдельное включение daily timer напоминаний.

## Ограничения

- Website не менять.
- Production не менять до staging-проверки.
- Daily timer напоминаний пока не установлен и не включен.
- Не печатать токены и приватные ключи.
- Не смешивать изменения с грязным worktree `D:\Projects\WHIEDA`.
