# Состояние инженерного пакета

Задача: `engineering-order-20260905` — порядок разработки и автоматические
защиты. Владелец: Виктор. Лид принимает каждый блок отдельно.
Обновлено: 2026-09-05, после блока A.

Это контекст, не альтернативный Git. Машинный снимок Git — в
`GIT_SNAPSHOT.md`, он пересоздаётся командой и всегда важнее текста здесь.

## Где идёт работа

| | Значение |
|---|---|
| Корневой repo | `D:/Projects/WHIEDA`, ветка `wip/consolidation-20260831` |
| Repo сайта | отдельный: `D:/Projects/WHIEDA/03_Website/wwc-best` |
| Task-worktree сайта | `D:/Projects/_worktrees/wwc-engineering-order-20260905` |
| Ветка task-worktree | `chore/engineering-order-20260905` |
| Base SHA | `bb603a97b19af2f9e6f647d202c0eb1999bab077` (= `origin/master`) |

**Почему такая база.** `bb603a9` — опубликованный master; production `9044e52`
является его предком (5 commits между ними, verified 2026-09-05
`git merge-base --is-ancestor`). База не содержит скоростного среза
(`fe4e0ac`, `258ec39`, `e89d425`), который ТЗ велит в этот пакет не включать.
**Выбор интеграционной базы блок A не назначает — это решение лида.**

## Разрешённые пути

Блок A изменял только:

- `tools/` в корневом repo (новый каталог: doc-check, git-snapshot, README)
- `PROCESS/engineering-order-20260905/` (документы этого пакета)

Runtime-код, цены, фото, стили, ветки и серверы не изменялись. Task-worktree
сайта создан, но пока пуст по изменениям — он нужен блокам B и C.

## Что сделано в блоке A

1. Task-worktree от явной базы — создан.
2. `GIT_SNAPSHOT.md` — генерируется `tools/git-snapshot.mjs`, не пишется руками.
3. Реестр worktree — в том же снимке: 24 в корневом repo, 20 в repo сайта.
   Владелец каждого — `unknown`, ничего не удалялось.
4. `ENVIRONMENTS.md` — карта сред с `verified` / `reported` / `unverified` и
   списком проверок для лида. Онлайн-проверки в этом блоке не выполнялись.
5. `SOURCE_MAP.md` — карта источников по шести объектам плюс четыре найденных
   конфликта с предложениями.
6. `tools/doc-check/` — проверка обязательных документов и ссылок, манифест,
   четыре негативные фикстуры, пять тестов.
7. `TASK_TEMPLATE.md`, `tools/README.md` с точными командами.

## Чужие срезы — не трогать

В repo сайта появился worktree
`C:/Users/srs/Documents/WHIEDA/.tmp/wwc-block-c-20260905`, ветка
`review/astra-block-c-20260905`, 3 уникальных commit от базы. Это отдельный
исполнитель по блоку C ТЗ ревизии. Не переключать, не сливать, не удалять.

Скоростной срез живёт в `D:/Projects/_worktrees/wwc-staging-referral-isolation`,
ветка `fix/staging-referral-isolation` @ `e89d425`. Общие `scripts/` и
`package.json` сайта заняты им — поэтому инструменты этого пакета сделаны
отдельно, в корневом `tools/`, и в `release:verify` не подключаются.

## Открытые вопросы к лиду

1. Какая база интеграционная и кто владелец выпуска.
2. Перенос правил в остальные рабочие копии сайта — согласованным Git-пакетом.
   Сейчас канон утверждает наличие `check:seo`, которого нет в основном
   checkout сайта; doc-check это ловит двумя ошибками.
3. Шесть read-only проверок на сервере — список в конце `ENVIRONMENTS.md`.
4. Четыре конфликта источников — в конце `SOURCE_MAP.md`, каждый с одним
   предложением.

## Следующий шаг

Лид принимает блок A. После приёмки — блок B (три конвейера и UI-границы) в
task-worktree, затем блок C (change-scope validator и релизная приёмка).

## Что НЕ проверялось

Production, staging, SSH, Sheets, БД в этом блоке не проверялись и не
менялись. Значения с пометкой `verified (perf-срез)` в `ENVIRONMENTS.md`
получены раньше в отдельном скоростном срезе того же дня, метод указан рядом.
